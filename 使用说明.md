# DeepSeek Harness 桌面版 · 使用说明

> 版本 2.0 · macOS（Apple Silicon / Intel 均可）· 更新日期 2026-08-14

---

## 一、这是什么

**DeepSeek Harness** 是一个本地 AI 开发工具（服务端口 `http://127.0.0.1:3080`）。
本桌面版是一个原生 macOS 应用，负责帮你一键启动 / 停止这个服务，并自动在浏览器中打开界面。

它替代了在终端里手敲 `npx @deepseek-ai/dsh web` 的方式，双击即可使用，并常驻程序坞与菜单栏，随时可控制。

---

## 二、安装

### 方式一：通过 DMG（推荐）

1. 双击打开 `DeepSeek Harness.dmg`
2. 把 **DeepSeek Harness.app** 拖到 **Applications** 文件夹（或桌面）
3. 首次打开时，如果系统提示"无法验证开发者"：
   - 右键（或按住 Control 点击）App → **打开** → 再点 **打开**
   - 或在终端执行：`xattr -dr com.apple.quarantine ~/Desktop/DeepSeek\ Harness.app`

### 方式二：直接使用 App

双击 App 即可运行，无需安装到 Applications（推荐拖到 Applications 更整洁）。

---

## 三、快速上手

1. **双击 `DeepSeek Harness.app`**
2. 控制面板窗口自动弹出，显示 `◐ 正在启动…`
3. 服务就绪后自动打开浏览器访问 `http://127.0.0.1:3080`，状态变为 `● 运行中`
4. 用完点 **退出** 或 **停止服务** 即可（都会先停掉服务进程）

> 💡 如果 3080 端口已经有服务在运行（比如你手动启动过），应用会直接打开浏览器，不会重复启动。

---

## 四、三处操作入口

| 入口 | 位置 | 作用 |
|---|---|---|
| **控制面板窗口** | 双击 App 自动弹出 | 状态显示 + 四个按钮：打开浏览器 / 启动·停止服务 / 查看日志 / 退出 |
| **程序坞图标** | Dock 栏，白底鲸鱼图标 | 点击重新弹出面板；右键 → 退出 |
| **菜单栏小鲸鱼** | 屏幕右上角菜单栏 | 同样的功能菜单，不占屏幕 |

### 控制面板按钮说明

| 按钮 | 快捷键 | 说明 |
|---|---|---|
| 打开浏览器 | ⌘O | 打开 http://127.0.0.1:3080 |
| 启动服务 / 停止服务 | ⌘S | 一键启停（按钮文字随状态变化） |
| 查看日志 | ⌘L | 用系统默认编辑器打开日志文件 |
| 退出 | ⌘Q | 停止服务并退出应用 |

> 菜单栏小鲸鱼菜单里有相同的选项，另有 **显示控制面板**（⌘P）。

---

## 五、常见问题（FAQ）

### Q1：双击后没反应 / 窗口没弹出？
- 先看程序坞有没有鲸鱼图标：如果 App 已在运行，再次双击只会把已有窗口调到前台（单实例设计）。
- 如果确实没启动，打开日志看原因（见 Q4）。

### Q2：服务启动失败？
- 打开日志（面板 → 查看日志），定位 `服务进程已退出` 附近的报错。
- 常见原因：`node`/`npx` 未安装或不在默认路径。本应用会自动补全 `/usr/local/bin`、`/opt/homebrew/bin` 等常见路径。

### Q3：提示 "Cannot find the native Koffi module" 或 "Could not load the sharp module"？
这是 dsh 原生模块（图像库 sharp、沙箱库 koffi）缺失的报错。
本应用已内置双架构（arm64 + x64）模块，正常情况下不会出现；
若出现，通常是 npx 缓存损坏，可在终端执行：
```bash
rm -rf ~/.npm/_npx/*/node_modules/@deepseek-ai/dsh
npx @deepseek-ai/dsh web
```
重新下载后即可。

### Q4：日志在哪里？
- `~/Library/Logs/DeepSeek-Harness.log`（应用自身日志 + dsh 服务输出都在这里）
- 面板或菜单栏点 **查看日志** 直接打开

### Q5：端口 3080 被占用怎么办？
- 应用会自动检测：端口已被占用时直接打开浏览器，不重复启动。
- 想强制重启：点 **停止服务**（会停掉 3080 上的服务），再点 **启动服务**。

### Q6：为什么菜单栏/程序坞图标有时候不显示？
- 程序坞：应用运行期间一直显示，退出后消失，属正常现象。
- 菜单栏：系统在"完全控制"等场景下可能自动隐藏部分菜单栏图标，可到 系统设置 → 控制中心 → 菜单栏 里调整。

### Q7：卸载
- 把 `DeepSeek Harness.app` 拖进废纸篓即可。
- 可选清理：日志文件 `~/Library/Logs/DeepSeek-Harness.log`、dsh 配置 `~/.dsh`。

---

## 六、技术说明（进阶）

| 项目 | 说明 |
|---|---|
| 运行方式 | 原生 Swift/AppKit 应用（非脚本壳），**通用二进制：arm64 + x86_64**，支持 Intel 与 Apple Silicon |
| 系统要求 | macOS 11 (Big Sur) 及以上 |
| 运行依赖 | Node.js（npx），首次运行自动下载 dsh 依赖 |
| 服务命令 | `npx @deepseek-ai/dsh web`，由应用托管（非后台孤儿进程） |
| 工作目录 | `~/Documents/AI agent`（不存在时回退家目录；可在源码 `main.swift` 顶部 `WORK_DIR` 修改） |
| 架构处理 | 自动以本机原生架构运行；双架构原生模块（arm64/x64）均可加载 |
| 单实例 | 按可执行文件路径检测，重复双击只会调起已有实例 |
| 退出行为 | 退出/停止时先停掉托管的服务进程，并兜底结束占用 3080 的进程 |
| 源文件 | `dsh-app-builder/`：`native/main.swift`（应用源码）、`make_icon.py`、`compose_icon_white.py`（图标生成）等 |

---

## 八、在其他 Mac 上使用（兼容性说明）

这个 DMG 是**通用版**（Intel 与 Apple Silicon 双架构），但换一台 Mac 首次使用前请确认：

1. **安装 Node.js（必需）**：应用通过 `npx` 启动 dsh 服务。到 [nodejs.org](https://nodejs.org) 下载 LTS 版，或用 Homebrew（`brew install node`）/ nvm 安装。应用会自动识别 Homebrew（`/usr/local`、`/opt/homebrew`）和 nvm（`~/.nvm`）的常见安装位置。
2. **macOS 11 (Big Sur) 或更高**。
3. **首次启动需要联网**：第一次点"启动服务"时，npx 会自动下载 dsh 及其依赖（几十 MB），请耐心等待；之后不再重复下载。
4. **首次打开提示"无法验证开发者"**：应用为本地 ad-hoc 签名（未购买苹果开发者证书）。右键（或按住 Control 点击）App → **打开** → 再点 **打开**；或终端执行 `xattr -dr com.apple.quarantine "/Applications/DeepSeek Harness.app"`。
5. **工作目录自适应**：默认在 `~/Documents/AI agent` 启动服务，目录不存在时自动回退到家目录，不影响使用。

> 📣 如果要**分发**给很多人，建议申请苹果 **Developer ID 证书并做公证（notarization）**，其他 Mac 首次打开就不会有任何拦截提示。

---

## 九、更新记录

- **v2.1**：跨机器兼容。改为**通用二进制**（Intel + Apple Silicon 双架构）、最低支持 macOS 11；PATH 自适应 Homebrew/nvm；工作目录不存在时自动回退；文档新增"在其他 Mac 上使用"章节。
- **v2.0**：原生应用版。新增控制面板窗口、菜单栏鲸鱼图标、单实例保护、一键启停、查看日志；修复 Rosetta 架构下原生模块缺失导致的启动失败；修复单实例检测误判孤儿进程的 bug。
- **v1.x**：壳脚本版（已废弃，被 v2.0 取代）。
